<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <title>Editar</title>
  <style>
    body {
      background-color: #f8f9fa;
      color: #343a40;
    }

    h1 {
      color: #343a40;
      margin-bottom: 20px;
    }

    form {
      margin-bottom: 20px;
    }

    label {
      font-weight: bold;
    }

    input[type="text"],
    input[type="email"],
    input[type="password"],
    button[type="submit"] {
      margin-bottom: 10px;
      border-radius: 4px;
      padding: 8px;
    }

    a {
      display: inline-block;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <br>
  <div class="container">
    <h1 class="font-italic">Editar Usuario</h1>
    <form action="/users/update/<%= user._id %>" method="POST">
      <div class="form-group">
        <label for="name" class="font-italic">Nombre:</label>
        <input type="text" id="name" name="name" value="<%= user.name %>" required class="form-control">
        <div class="invalid-feedback"></div>
      </div>
      <div class="form-group">
        <label for="email" class="font-italic">Email:</label>
        <input type="email" id="email" name="email" value="<%= user.email %>" required class="form-control">
        <div class="invalid-feedback"></div>
      </div>
      <div class="form-group">
        <label for="password" class="font-italic">Contraseña:</label>
        <input type="password" id="password" name="password" placeholder="Si quiere seguir con la contraseña actual no llenar este campo" class="form-control">
      
      </div>
      <button type="submit" class="btn btn-warning" >Actualizar Usuario</button>
    </form>
    <hr>
    <a href="/users" class="btn btn-info">Regresar a la lista de usuarios</a>
  </div>
</body>
</html>
